const { Server } = require('./server')
const {
  METADATA_EXTENSION,
  SIGNAL_EXTENSION,
  KEY_EXTENSION,
  NAMESPACE,
} = require('./extension')

/**
 * Module exports.
 */
module.exports = {
  METADATA_EXTENSION,
  SIGNAL_EXTENSION,
  KEY_EXTENSION,
  NAMESPACE,
  Server
}
